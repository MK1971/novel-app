<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Landing UX suggestions — local</title>
    <style>
        * { box-sizing: border-box; }
        body {
            margin: 0;
            padding: 2rem 1.5rem 4rem;
            font: 15px/1.6 ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
            color: #1c1917;
            background: #fafaf9;
            max-width: 52rem;
            margin-left: auto;
            margin-right: auto;
        }
        h1 {
            font-family: system-ui, -apple-system, sans-serif;
            font-size: 1.35rem;
            font-weight: 800;
            margin: 0 0 0.5rem;
            letter-spacing: -0.02em;
        }
        .meta {
            font-size: 0.8rem;
            color: #78716c;
            margin-bottom: 1.5rem;
            font-family: system-ui, -apple-system, sans-serif;
        }
        pre {
            margin: 0;
            white-space: pre-wrap;
            word-break: break-word;
            padding: 1.25rem 1.5rem;
            background: #fff;
            border: 1px solid #e7e5e4;
            border-radius: 0.75rem;
            box-shadow: 0 1px 2px rgba(0,0,0,0.04);
        }
    </style>
</head>
<body>
    <h1>Landing page UX / UI suggestions</h1>
    <p class="meta">Source: <code>docs/landing-ux-suggestions.txt</code> · Local only · Edit the text file to update this view.</p>
    <pre>{{ $content }}</pre>
</body>
</html>
